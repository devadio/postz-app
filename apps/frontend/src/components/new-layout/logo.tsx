'use client';

import React from "react";

export const Logo = () => {
  return (
    <img
      src="/postiz-text.svg"
      alt="Logo"
      width={60}
      height={60}
      className="mt-[8px]"
    />
  );
};

export default Logo;
